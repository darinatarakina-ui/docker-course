# Упражнение 1.1

Запустила три контейнера:

docker run -d nginx
docker run -d nginx
docker run -d nginx

Остановила два контейнера:

docker stop 56e3 4b08

Результат:

docker ps -a

Вывод:

CONTAINER ID   IMAGE     STATUS
56e338ff675b   nginx     Exited (0)
4b08ec3a3212   nginx     Exited (0)
d039b71065c9   nginx     Up

Один контейнер работает, два остановлены.
